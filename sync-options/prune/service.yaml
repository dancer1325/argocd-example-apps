apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Prune=true
  name: guestbook-ui-prune-confirm
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook-ui
